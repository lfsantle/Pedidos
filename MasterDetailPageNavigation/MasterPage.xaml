<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
			 x:Class="MasterDetailPageNavigation.MasterPage"
			 Padding="0,20,0,0"
			 Icon="hamburger.png"
			 Title="Pedidos TOL"
			 BackgroundColor="#1c2d3d"
>
	<ContentPage.Content BackgroundColor="#1c2d3d">

		<StackLayout>

			<StackLayout VerticalOptions="Start" Padding="20" BackgroundColor="#152433">
				<Label Text="Bem vindo, " TextColor="#ffffff" />
			</StackLayout>
			
			<StackLayout VerticalOptions="CenterAndExpand" Padding="20" BackgroundColor="#1c2d3d">
				<Label Text="Pedidos TOL" Margin="5,5,5,5" TextColor="#ffffff" FontSize="18" ></Label> 
				<ListView x:Name="listView" VerticalOptions="FillAndExpand" SeparatorVisibility="None" BackgroundColor="#1c2d3d">
					<ListView.ItemTemplate>
						<DataTemplate>
							<ImageCell Text="{Binding Title}" ImageSource="{Binding IconSource}" TextColor="#ffffff" />
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
			</StackLayout>	

			<StackLayout VerticalOptions="End" Padding="20" HorizontalOptions="Center">
				<Label Text="© 2017 | Liv3n | www.liv3n.com.br" Font="Small" TextColor="#ffffff" />
			</StackLayout>
				
		</StackLayout>
		
	</ContentPage.Content>
</ContentPage>
